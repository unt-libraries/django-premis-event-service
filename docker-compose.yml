db:
    image: mysql:latest
    environment:
        - MYSQL_ROOT_PASSWORD=root
        - MYSQL_DATABASE=premis

web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000 
    volumes:
        - .:/app/
    ports:
        - "8000:8000"
    links:
        - db 
